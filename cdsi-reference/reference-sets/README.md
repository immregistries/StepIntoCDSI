# Reference Sets (not yet built)

This directory will hold reviewed bindings that pin together an exact Logic Specification version, Supporting Data release, and `cdsi-fits-tests` fixture set, so every FITS run can record which combination it was interpreted against (Phase 16 of `StepIntoCDSi-Specification-Reference-Module-Plan.md`).

Not started yet - this depends on Supporting Data versioning (Phases 13-15) being in place first, which depends on the Logic Specification milestone (Phases 1-12) being complete. See the plan document at the repository root.
