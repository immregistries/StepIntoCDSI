# Repetition and Transition Diagrams (not yet built)

Mermaid diagrams for the major processing loops (Phase 7 of the plan) come after Phase 5's full Chapters 4-9 extraction. Each pilot step package's own `transitions.yaml` (see `../steps/*/transitions.yaml`) already records that step's own outgoing transitions in structured form - this directory is for the larger, multi-step loop diagrams built from those once more steps are documented.
