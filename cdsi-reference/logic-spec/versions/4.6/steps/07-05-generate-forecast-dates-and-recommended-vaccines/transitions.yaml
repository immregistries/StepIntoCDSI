from: "7.5"
spec_defines_transition: false
note: >
  Verified against GenerateForecastDatesAndRecommendedVaccines.process(),
  which computes the forecast's dates and transitions unconditionally.
transitions:
  - condition: "always"
    to: "7.6"
    verified_in: "org.openimmunizationsoftware.cdsi.core.logic.GenerateForecastDatesAndRecommendedVaccines#process"
