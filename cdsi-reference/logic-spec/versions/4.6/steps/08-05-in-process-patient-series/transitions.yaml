from: "8.5"
spec_defines_transition: false
note: >
  Verified against InProcessPatientSeries.process(), which sets its next
  step unconditionally after scoring.
transitions:
  - condition: "always"
    to: "8.7"
    verified_in: "org.openimmunizationsoftware.cdsi.core.logic.InProcessPatientSeries#process"
