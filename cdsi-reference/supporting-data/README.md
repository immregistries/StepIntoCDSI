# Supporting Data (not yet built)

This directory will hold versioned, checksummed copies of CDSi Supporting Data releases (the XML/XSD schedule data `cdsi-engine` loads) plus a deterministic normalizer, following Phases 13-15 of `StepIntoCDSi-Specification-Reference-Module-Plan.md`.

Not started yet - the Logic Specification milestone (Phases 1-12) comes first. See the plan document at the repository root for the full design (release manifests, normalized JSON/YAML by antigen/schedule/patient-series/etc., semantic diffing between releases).
